<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SkyCD.UI.Controls.Selectors.ColorPaletteDropdown.ColorPaletteDropdownControl"
             x:Name="Root">
    <Grid>
        <Button x:Name="ToggleButton"
                Click="OnToggleButtonClick"
                HorizontalContentAlignment="Stretch"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Height="32">
            <Border BorderBrush="{DynamicResource ThemeBorderMidBrush}"
                    BorderThickness="1"
                    Background="{DynamicResource ThemeBackgroundBrush}"
                    CornerRadius="4"
                    Padding="8,6">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                    <Border Width="16"
                            Height="16"
                            CornerRadius="2"
                            BorderBrush="#9CA3AF"
                            BorderThickness="1"
                            Background="{Binding SelectedBrush, ElementName=Root}" />
                    <TextBlock Grid.Column="1"
                               VerticalAlignment="Center"
                               Text="{Binding SelectedColor, ElementName=Root}" />
                    <TextBlock Grid.Column="2"
                               VerticalAlignment="Center"
                               Text="▾"
                               Foreground="#9CA3AF" />
                </Grid>
            </Border>
        </Button>

        <Popup x:Name="PalettePopup"
               PlacementTarget="{Binding #ToggleButton}"
               Placement="BottomEdgeAlignedLeft"
               IsOpen="False"
               IsLightDismissEnabled="True"
               Topmost="False">
            <Border Background="{DynamicResource ThemeBackgroundBrush}"
                    BorderBrush="{DynamicResource ThemeBorderMidBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10">
                <StackPanel Spacing="10">
                    <TextBlock Text="Color"
                               Foreground="{DynamicResource ThemeForegroundBrush}"
                               FontSize="12"
                               FontWeight="SemiBold" />
                    <Border x:Name="SvSurfaceBorder"
                            Width="200"
                            Height="140"
                            BorderBrush="{DynamicResource ThemeBorderMidBrush}"
                            BorderThickness="1"
                            HorizontalAlignment="Left">
                        <Grid x:Name="SvSurface"
                              PointerPressed="OnSvSurfacePointerPressed"
                              PointerMoved="OnSvSurfacePointerMoved"
                              PointerReleased="OnSvSurfacePointerReleased">
                            <Image x:Name="SvImage"
                                   Stretch="Fill" />
                            <Canvas>
                                <Border x:Name="SvThumb"
                                        Width="10"
                                        Height="10"
                                        BorderBrush="#FFFFFF"
                                        BorderThickness="1.5"
                                        CornerRadius="5"
                                        Background="#00000000" />
                            </Canvas>
                        </Grid>
                    </Border>

                    <TextBlock Text="Hue"
                               Margin="0,2,0,0"
                               Foreground="{DynamicResource ThemeForegroundBrush}"
                               FontSize="12"
                               FontWeight="SemiBold" />
                    <Border x:Name="HueBarBorder"
                            Width="200"
                            Height="12"
                            BorderBrush="{DynamicResource ThemeBorderMidBrush}"
                            BorderThickness="1"
                            HorizontalAlignment="Left">
                        <Grid x:Name="HueBarSurface"
                              PointerPressed="OnHueBarPointerPressed"
                              PointerMoved="OnHueBarPointerMoved"
                              PointerReleased="OnHueBarPointerReleased">
                            <Image x:Name="HueImage"
                                   Stretch="Fill" />
                            <Canvas>
                                <Border x:Name="HueThumb"
                                        Width="8"
                                        Height="12"
                                        BorderBrush="#FFFFFF"
                                        BorderThickness="1"
                                        Background="#00000000" />
                            </Canvas>
                        </Grid>
                    </Border>

                    <TextBlock Text="Hex"
                               Margin="0,2,0,0"
                               Foreground="{DynamicResource ThemeForegroundBrush}"
                               FontSize="12"
                               FontWeight="SemiBold" />
                    <TextBox x:Name="HexTextBox"
                             Width="200"
                             HorizontalAlignment="Left"
                             TextInput="OnHexTextBoxTextInput"
                             TextChanged="OnHexTextChanged" />
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
